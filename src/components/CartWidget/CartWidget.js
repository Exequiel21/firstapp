import React from 'react';
import './CartWidget.css';

const CartWidget = () => {
    return (
        <div>
            <img src='../assets/iconos/carrito.png' alt='carrito' className='cart' />
        </div> 
    );
};

export default CartWidget;
